# <Project> — router

<One-paragraph what-this-is + stack, verified.>

**All documentation lives in [`project-os/`](project-os/DIRECTORY.md)** — start at its
DIRECTORY and read only what the "Reference when" column points you to. The architecture
reference (`project-os/engineering/architecture.md`) is the source of truth for stack/structure
questions — read it BEFORE re-reading the code.

- Build/run/verify: see `project-os/ops/verify.md` (the VERIFY_CMD contract).
- Code layout: <2-4 bullet map of the real top-level code dirs — pointers, not prose>.
- Conventions for docs (frontmatter, citations): `project-os/CLAUDE.md` (auto-loads on demand).

<!-- art_is_everywear variant ONLY: keep `@AGENTS.md` as the FIRST line of the file (the
     Next.js toolchain regenerates AGENTS.md; never migrate or edit it), then this body. -->
